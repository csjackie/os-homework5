Name: Jackie Herbstreit
Date: April 8th, 2026
Environment: linux, vim
How to compile the project:
	Type 'make'
Example of how to run the project:
	./oss -n 3 -s 2 -t 4 -i 0.6 -f log.txt

Generative AI used: chatpgt
Prompts:Explain how deadlock detection works in an operating system using a resource allocation graph and how it could be implemented in a simulator like this project.
	Help me design the data structures for an OS simulator that tracks processes, resource allocation, and blocked processes in C++.
	What modules should I separate in a C++ OS simulator using shared memory and message queues?
	How should the main oss loop coordinate process scheduling, resources allocation, and deadlock detection?
	Show how to implement a shared memory logical clock in C++ for multiple processes.
	What is a good struct design for shared memory in an OS simulator?
	What conditions determine whether a resource request should be granted or blocked?
	My deadlock detection is triggering incorrectly - what are common logic mistakes?
Summary: All answers given to prompts were extremely helpful in understanding the logic of the project. The answers were also helpful in organizing and fixing mistakes. 
